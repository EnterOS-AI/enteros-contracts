# molecule-contracts

Tool-Contract SSOT — single source of truth for capability/tool + template contracts (RFC molecule-core#3285). Public tier: third-party-facing schemas + the plugin SDK. WIP (P2).